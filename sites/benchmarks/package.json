{
  "name": "@microsoft/fast-bench",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "bundle": "vite build",
    "serve": "vite --clearScreen false",
    "start": "node server/server.mjs",
    "bench": "vite build && node src/bench.ts"
  },
  "dependencies": {
    "@microsoft/fast-build": "^0.9.0",
    "@microsoft/fast-element": "^3.0.1"
  }
}
