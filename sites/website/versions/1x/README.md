# FAST 1.x documentation

This package contains the FAST 1.x documentation source consumed by
`@microsoft/fast-site`. Its filesystem directory is `1x`, while its public URL remains
`/docs/1.x`. It is not independently published.

Build only this version from the repository root:

```sh
npm run build -w @microsoft/fast-site-1x
```
