# FAST 2.x documentation

This package contains the FAST 2.x documentation source consumed by
`@microsoft/fast-site`. Its filesystem directory is `2x`, while its public URL remains
`/docs/2.x`. It is not independently published.

Build only this version from the repository root:

```sh
npm run build -w @microsoft/fast-site-2x
```
