# FAST 3.x documentation

This package contains the FAST 3.x documentation source consumed by
`@microsoft/fast-site`. Its filesystem directory is `3x`, while its public URL remains
`/docs/3.x`. It is not independently published.

Build only this version from the repository root:

```sh
npm run build -w @microsoft/fast-site-3x
```
