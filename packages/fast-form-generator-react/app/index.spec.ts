import * as React from "react";
import * as App from "./index";

describe("Application", (): void => {
    test("should convert ids to component names", () => {
        // should test the method
    });
});
