{
    "$schema": "http://json-schema.org/schema#",
    "title": "Component with align",
    "description": "A test component's schema definition.",
    "type": "object",
    "id": "align",
    "properties": {
        "align": {
            "title": "Align",
            "type": "string",
            "enum": [
                "top",
                "bottom",
                "center"
            ],
            "disabled": true
        },
        "optionalAlign": {
            "title": "Align",
            "type": "string",
            "enum": [
                "top",
                "bottom",
                "center"
            ]
        }
    },
    "required": [
        "align"
    ]
}
