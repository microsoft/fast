{
    "$schema": "http://json-schema.org/schema#",
    "title": "Component with text align",
    "description": "A test component's schema definition.",
    "type": "object",
    "id": "textAlign",
    "properties": {
        "textAlign": {
            "title": "Text align",
            "type": "string",
            "enum": [
                "left",
                "center",
                "right"
            ]
        },
        "optionalTextAlign": {
            "title": "Optional text align",
            "type": "string",
            "enum": [
                "left",
                "center",
                "right"
            ]
        }
    },
    "required": [
        "textAlign"
    ]
}
