export enum Icon {
    play = "play",
    pause = "pause",
    robot = "robot",
    user = "user",
    arrow = "arrow",
    download = "download",
    favorite = "favorite",
    folder = "folder",
}

export interface GlyphProps {
    path: Icon;
}
