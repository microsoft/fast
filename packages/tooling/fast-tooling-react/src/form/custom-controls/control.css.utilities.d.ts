import React from "react";
import {
    RenderRefControlConfig,
    RenderSelectControlConfig,
} from "./control.css.utilities.props";
export declare function renderDefault(config: RenderRefControlConfig): React.ReactNode;
export declare function renderTextInput(config: RenderRefControlConfig): React.ReactNode;
export declare function renderNumber(config: RenderRefControlConfig): React.ReactNode;
export declare function renderInteger(config: RenderRefControlConfig): React.ReactNode;
export declare function renderCheckbox(config: RenderRefControlConfig): React.ReactNode;
export declare function renderSelection(
    config: RenderSelectControlConfig
): React.ReactNode;
export declare function renderColorPicker(
    config: RenderRefControlConfig
): React.ReactNode;
