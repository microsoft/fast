{
    "$schema": "http://json-schema.org/schema#",
    "title": "Radio",
    "description": "A radio component's schema definition",
    "type": "object",
    "id": "radio",
    "properties": {
        "tag": {
            "title": "HTML tag",
            "type": "string",
            "enum": [
                "div",
                "label"
            ],
            "default": "div"
        }
    }
}