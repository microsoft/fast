export {
    type AccessCachedPath,
    type CachedPath,
    type CachedPathCommon,
    type CachedPathMap,
    type ChildrenMap,
    type DefaultCachedPath,
    type EventCachedPath,
    type JSONSchema,
    type JSONSchemaDefinition,
    type RegisterPathConfig,
    type RepeatCachedPath,
    Schema,
    defsPropertyName,
    fastContextMetaData,
    fastContextsMetaData,
    refPropertyName,
} from "@microsoft/fast-element/extensions/schema.js";

