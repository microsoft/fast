<%_ const upperName = _.upperFirst(name) _%>
import { <%= upperName %> } from "./<%= name %>"
import { <%= name %>Styles as styles } from "./<%= name %>.styles";
import { <%= name %>Template as template } from "./<%= name %>.template"

export const fast<%= upperName %> = <%= upperName %>.compose({
    baseName: "<%= name %>",
    template,
    styles
})

export const <%= name %>Styles = styles;

export {<%= upperName %>}