<%_ const klass = _.upperFirst(_.camelCase(name.split(':').slice(-1).join(':'))) _%>
export default {
    title: "<%= klass %>",
};

const <%= klass %>Template = () => `
    <fast-<%= name %>>Fast <%= _.upperFirst(name) %></fast-<%= name %>>
`

export const <%= klass %> = <%= klass %>Template.bind({});
