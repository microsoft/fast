import { SwatchRGB } from "../swatch.js";

/**
 * A {@link SwatchRGB} convenience for white.
 *
 * @internal
 */
export const _white = SwatchRGB.create(1, 1, 1);

/**
 * A {@link SwatchRGB} convenience for black.
 *
 * @internal
 */
export const _black = SwatchRGB.create(0, 0, 0);
