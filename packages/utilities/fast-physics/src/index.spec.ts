import { expect } from "chai";
import { FastPhysicsPlaceholder } from "./";

describe("Badge", () => {
    it("should be null", async () => {
        expect(FastPhysicsPlaceholder).to.be.null;
    });
});
