export interface IFontWeight {
    light: number;
    semilight: number;
    normal: number;
    semibold: number;
    bold: number;
}

export const fontWeight: IFontWeight = {
    light: 100,
    semilight: 200,
    normal: 400,
    semibold: 600,
    bold: 700
};
